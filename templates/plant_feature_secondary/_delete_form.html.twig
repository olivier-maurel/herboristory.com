<form method="post" action="{{ path('app_plant_feature_secondary_delete', {'id': plant_feature_secondary.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plant_feature_secondary.id) }}">
    <button class="btn">Delete</button>
</form>
