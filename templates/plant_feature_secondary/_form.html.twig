{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success">{{ button_label|default('Save') }}</button>
    <button class="btn btn-danger" data-role="delete" style="display: none;">{{ button_label|default('Delete') }}</button>
{{ form_end(form) }}
